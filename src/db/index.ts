import DBConstructor from './core.js';

// https://www.npmjs.com/package/node-json-db
export const nodeVersion = parseInt(process.version.toLowerCase().replace('v', ''));

export default DBConstructor;
